# RELATIONAL KEY — COLLECTION CONSOLIDATION C3

Date: 2026-08-25
Branch: `collection/consolidation-gate-1`

# COLLECTION SHELL — IMPLEMENTED / SOURCE PASS

Implementation commit:
`f7416092c64fd7fd17b5771f002fdae5b0e1433f`

## Purpose

Transform the repository root from the last individual-family prototype into a collection-level entrance without modifying any validated family snapshot.

The shell must make the system legible before a visitor enters an individual family.

Canonical invariant remains:

# **THE RELATIONAL PAIR REMAINS THE PRODUCT.**

## Implemented collection experience

Root `index.html` now provides:

- RELATIONAL KEY collection identity;
- hero thesis: `The pair is the product.`;
- explicit relationship proof: `OBJECT A → RELATION → OBJECT B`;
- Continental Balance Gate 1 overview;
- locked distribution `4 / 3 / 2 / 2 / 2 / 2`;
- collection atlas generated from `collection/families.json`;
- all 15 family entries;
- continent filters;
- full-text search across place / memory / mechanism / law;
- direct route navigation into each frozen family snapshot;
- responsive single/two/three-column collection layout;
- collection-level framing only — no family mechanism replacement.

## Visual strategy

The shell deliberately avoids cultural collage or decorative cultural skinning.

Family cards use an abstract two-object + gap diagram so the **relationship architecture** remains the shared visual grammar while cultural specificity stays inside the family experience itself.

This prevents the collection home from flattening fifteen different cultural mechanisms into superficial thumbnails.

## Source integrity verification

C3 commit inspection confirms the implementation modifies the root `index.html` only.

Therefore the C2 family snapshot tree remains untouched by the shell implementation.

Registry verification:
- `validated_count = 15`
- `candidate_count = 0`
- fifteen family records
- six continent totals remain `4 / 3 / 2 / 2 / 2 / 2`.

C2 regression anchors remain unchanged:
- autonomous snapshot import commit `e7e1473b0d789fa09b33e2e5b8961c33f32227d2`;
- restored shared-runtime import commit `9321844cac8c2e9ccedda7e206b1ee03445bed83`.

## Gate status

# **C3 = IMPLEMENTED / SOURCE PASS / LIVE PREVIEW REQUIRED**

This is not yet a collection visual pass.

`SOURCE PASS ≠ LIVE ROUTE PASS ≠ COLLECTION VISUAL PASS`.

## Next gate — C3.1 Dedicated consolidation live

1. use one dedicated Vercel collection project;
2. deploy `collection/consolidation-gate-1`;
3. verify root registry load;
4. verify all 15 direct family routes return successfully;
5. spot-check the six shared-runtime launch routes initialize the intended family;
6. preserve all historic family Vercel deployments as regression references;
7. request one collection-level user recording / usability audit.

Global Refinement remains blocked until the collection shell itself passes live review.
