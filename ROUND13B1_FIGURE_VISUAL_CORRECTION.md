# ROUND 13B.1 — Cultural Figure Visual Correction

Status: **IMPLEMENTED / DEPLOYED — BROWSER RECHECK REQUIRED**

Triggered by browser recording review.

Observed issues from the recording:
1. Primary generated card texture failed to render in browser, leaving a blank front surface.
2. Memory field was too diagrammatic relative to the intended tactile collectible object.
3. Registration junction read as a floating UI control instead of a seam between two objects.
4. Outcome typography became larger than the artifact and disrupted hierarchy.
5. Front / Back / Macro and claim controls were too small and visually detached.
6. Macro and Back states read as interface modes, not physical inspection modes.

Correction law:
**OBJECT FIRST → RELATION SECOND → EXPLANATION THIRD**

Implemented corrections:
- absolute pinned generated texture URL plus designed fallback surface
- richer four-part trace archive field
- junction rebuilt as a seam with rails connecting artifact and counterpart field
- outcome reduced to a compact subordinate plaque
- rounded, larger physical-view controls
- front/back/macro retained as inspection modes with stronger object hierarchy
- restrained no-match recoil; credential remains valid
- direct static Vercel bundle used instead of the previous loader architecture

Asset rule:
- generated prototype surface only
- no claim that generated imagery is archival or official
- archive sources inform mechanism/context, not surface authenticity

Current deployment candidate:
`relational-key-cultural-figure-frida-v131`

Promotion gate remains open until browser recording confirms:
- front visual actually renders
- seam reads as belonging to both objects
- archive field is visually rich enough next to the credential
- outcome no longer dominates
- front/back/macro feel physical
- matching/no-match remain understandable
