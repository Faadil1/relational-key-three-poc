# ROUND 13M.6 — USER VISUAL AUDIT — PASS / PROMOTE

Date: 2026-08-25
Family: **COMPLEMENT MEMORY — FRANCE / BOULLE — RECIPROCAL CUT REGISTER**

## User evidence

Uploaded screen recording audited end-to-end.
Observed media properties:
- duration: ~25.93 s
- resolution: 1908×962
- frame rate: 30 fps

The recording contains both required states:
1. `SHARED CUT → TEST RELATIONSHIP → CONTINUE`
2. `OTHER CUT → TEST RELATIONSHIP → CONTINUE`

## Verdict

# **VISUAL PASS / PROMOTE**

No blocking visual or code patch is required before the merge gate.

## SHARED CUT — PASS

The sequence visibly demonstrates the intended causal chain:

`ALIGN → STACK → CUT → SEPARATE → EXCHANGE → INVERT → REGISTER`

Verified from the recording:
- Card A and Card B begin as visibly different material fields;
- the same motif/cut contour becomes visible on both sides;
- the center field resolves the same shared division across both layers;
- the center layers visibly separate before the reciprocal result appears;
- exchange/inversion resolves as opposite material fills in the same vector region;
- Card A becomes dark ground + light inlay;
- Card B becomes light ground + dark inlay;
- both final cards are reciprocal material inverses, not copies;
- the pair remains visually dominant over the center field.

The proof is therefore perceptible as:

`ONE SHARED CUT → TWO COMPLEMENTARY INVERSES`

rather than merely being explained by labels.

## OTHER CUT — PASS

Verified from the same recording:
- the two fields remain visually valid;
- partial/offset paths appear rather than one common shared map;
- no reciprocal filled inlays emerge;
- no complete `PARTIE ↔ CONTREPARTIE` state registers;
- no red-X / error-dominant invalidation appears;
- the interaction returns to a neutral unresolved pair.

## Non-blocking polish note

The small exchange cue in the center is intentionally subtle; the strongest proof comes from the shared contour followed by the simultaneous reciprocal fills on the two cards. This is sufficient for promotion. If the collection later receives a global refinement pass, center microcopy / exchange markers may be enlarged slightly for small-screen legibility, but this is not a 13M blocker.

## Historical / material boundary

The audit does not change the existing boundary:
- historical tortoiseshell remains documentary evidence only;
- prototype materials are editorial simulations;
- no wildlife material is used or recommended;
- no exact modern fabrication recipe is claimed.

## Gate state

- research: PASS
- concept: PASS
- implementation: PASS
- live candidate: READY / source-live sync verified
- user visual audit: **PASS**
- promotion: **YES**
- merge: NOT YET — requires explicit merge approval

`VISUAL PASS / PROMOTE ≠ MERGED`.
