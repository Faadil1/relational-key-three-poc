# RELATIONAL KEY — CONTINENTAL BALANCE B2.3N

Date: 2026-08-28
Branch: `collection/global-refinement-1`
Spec: `CONTINENTAL_BALANCE_B2_3M_OCEANIA_GARAMUT_SPEC.md`

# GARAMUT AUTONOMOUS BUILD CANDIDATE + SOURCE GATE

Status:
# **SOURCE PASS / LIVE + USER VISUAL+AUDIO GATE PENDING**

Candidate:
`families/garamut-sepik-ramu/candidate.html`

Implementation commit:
`acb90a987f17c35ee94828f9433f5dd0055a0609`

Candidate blob:
`d78ec124d1111ba6a82137b58c0922fa7fdc1a36`

Spec commit:
`64359d37f5e7ceccbd7b244ed79a590951e3ab8f`

---

# Atomic source proof

Compared from canonical pre-build checkpoint:
`fe64a9f445a65b464c67967e2b2c3a3e6af0b950`

to implementation:
`acb90a987f17c35ee94828f9433f5dd0055a0609`

Result:
- ahead by 1 commit;
- exactly one changed path;
- `families/garamut-sepik-ramu/candidate.html` = added;
- **76 additions / 0 deletions**;
- no atlas change;
- no `collection/families.json` change;
- no public route change;
- no validated-family mutation.

---

# Exact candidate identity proof

Local candidate used for syntax validation:
`/tmp/garamut_candidate.html`

Local Git blob hash:
`d78ec124d1111ba6a82137b58c0922fa7fdc1a36`

GitHub candidate blob:
`d78ec124d1111ba6a82137b58c0922fa7fdc1a36`

# **BYTE-IDENTICAL SOURCE CONFIRMED**

The committed build is exactly the version that passed local JavaScript syntax validation.

---

# Product pair implementation

## Card A

`WOODEN POUNDER / INPUT MEMBER`

- bounded physical member;
- direct pointer/touch drag;
- keyboard fallback;
- source of all credited impact events.

## Card B

`GARAMUT / RESONANT SIGNAL BODY`

- bounded resonant member;
- plain slit-log structural rendering;
- no clan/spirit/ceremonial decoration;
- impact feedback remains inside Card B.

The center contains only a narrow editorial timing trace.

# **THE RELATIONAL PAIR REMAINS THE PRODUCT.**

---

# Direct strike + hysteresis proof

Editorial constants:
- `LIMIT = 112`;
- `STRIKE = 92`;
- `REARM = 46`.

A pointer strike is credited only when:
`armed == true AND x >= STRIKE`.

Immediately after credit:
`armed = false`.

A further pointer strike cannot be credited until:
`x <= REARM`.

Therefore pointer jitter near contact cannot create an uncontrolled burst of events.

No timer or autoplay generates strikes.

---

# Event-order / timing trace

Each credited strike records:
- absolute `performance.now()` timestamp;
- relative time from the first event;
- interval from previous event.

Trace behavior:
- one event: `ONE IMPACT · no signal-sequence claim yet`;
- two or more: `ORDERED EVENTS · user-authored timing now forms an editorial signal sequence`;
- maximum eight recent events shown in the narrow trace lane;
- no message is decoded;
- no sequence is marked correct or incorrect;
- no terminal success state exists.

Required visible label is present:
# `NOT COMMUNITY CODE`

---

# Keyboard fallback

`Space` / `Enter`:
- non-repeated keydown records one strike only when armed;
- repeated keydown ignored;
- keyup restores pounder offset and re-arms;
- uses the same `recordStrike()` event/audio/trace path as pointer input.

---

# Synthetic audio boundary

Each credited event produces a short browser-synthesized impact:
- oscillator type `triangle`;
- frequency approximately `96 Hz → 56 Hz` over `0.09 s`;
- gain approximately `.12 → .001` over `0.12 s`;
- stopped around `0.13 s`.

This is interaction feedback only.

Visible candidate wording:
# `SYNTHETIC IMPACT AUDIO · INTERACTION PROOF ONLY · NOT AUTHENTIC GARAMUT TIMBRE`

No sampled ceremonial audio and no authenticity claim.

---

# Safeguarding source gate

Visible primary boundary:

# `STRUCTURAL TRANSLATION · EAST SEPIK / RAMU GARAMUT SIGNALING RELATION · EDITORIAL EVENT TIMING ONLY · NOT COMMUNITY CODE`

The build contains:
- no warning/death/meeting/invitation/personal-name mapping;
- no translated real-world message;
- no universal-PNG-code claim;
- no clan/spirit decorative carving;
- no ceremony scene;
- no waveform hero;
- no faux Morse/telegraph metaphor;
- no target rhythm to pass/fail.

Every user-authored timing sequence remains an editorial sequence only.

---

# Reset

Reset:
- clears all captured events;
- restores pounder offset;
- restores armed state;
- clears trace;
- closes existing AudioContext where possible;
- restores `NO IMPACT EVENTS RECORDED`.

---

# Responsive / accessibility checks

Responsive CSS present at:
- `max-width: 760px`;
- `max-width: 470px`.

Source preserves:
- two side-by-side bounded member cards;
- narrow central trace;
- direct interaction at reduced sizes.

Accessibility/control:
- pounder is a native button;
- clear ARIA label explains drag/re-arm behavior;
- keyboard Space/Enter fallback present.

---

# Syntax gate

Exact local script:
`/tmp/garamut_candidate.js`

Command:
`node --check /tmp/garamut_candidate.js`

Result:
# **PASS**

Local `git hash-object` for the complete candidate exactly equals committed GitHub blob:
`d78ec124d1111ba6a82137b58c0922fa7fdc1a36`.

---

# B2.3N verdict

# **SOURCE PASS**

This does NOT equal:
- LIVE PASS;
- USER VISUAL+AUDIO PASS;
- VALIDATED FAMILY;
- atlas/registry promotion.

Validated Oceania remains:
# `2 / 4`

# Required live/user evidence later

1. two physical members remain visually primary;
2. direct pounder drag feels legible;
3. one approach yields one impact only;
4. withdrawal visibly re-arms;
5. keyboard fallback yields identical event path;
6. event trace stays subordinate;
7. synthetic impact audio is restrained and clearly non-authentic;
8. no cultural-code/message implication appears visually;
9. no target success/failure emerges;
10. narrow layout remains usable.

# Next required output

If no fresh deployable Preview is available:
# **B2.2I — EUROPE +2 SOURCE-FIRST CANDIDATE POOL**

If deployment capacity has returned, a descendant Preview may host the source-pass queue, but each candidate must still receive an independent live/user gate.
