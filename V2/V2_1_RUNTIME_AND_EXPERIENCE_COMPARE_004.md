# RELATIONAL KEY V2.1 — Human V1 ↔ V2 Comparison 004 / Three-Pilot Pattern Closeout

Date: 2026-09-04
Branch: `v2/archive-interaction-3d-matrix-001`

## Exact tested head

`c2bd305d90bde51f1d996fb0d516222e1d05a240`

## CI / artifact identity

Build / relational contract:
- run `33903973139`
- PASS

Exact-browser + Focus Experience 002:
- run `33903973187`
- PASS
- artifact `9948776748`
- digest `sha256:f37eb5660b42a5b01b7863c79e412cfa6f7a3b218a103e8c0d3e1436f4ea030f`

Focus evidence schema:
- `RELATIONAL_KEY_V2_FOCUS_CAPTURE_002`
- verdict `FOCUS_EXPERIENCE_CAPTURE_PASS_PENDING_HUMAN_V1_V2_COMPARISON`

## Exact pair identities proven in browser

### Anamorphosis · Paris
`DISTORTED FIELD → CURVED REFLECTION → CYLINDRICAL REFLECTOR`

### Coupler · Virginia
`PIVOTING HEAD → ROTARY HOOK / LATCH → MATING HEAD`

### Ombak · Bali
`PENGUMBANG → SHARED BEAT ENVELOPE → PENGISUP`

All three pass:
- explicit pair-member identity rail
- OTHER / MATCHING
- one Canvas
- desktop `1440 / 1440` no horizontal overflow
- mobile `390 / 390` no horizontal overflow
- no Focus findings

## Human comparison 004

### Anamorphosis · Paris

Verdict: `V2_PILOT_PROMOTION_CANDIDATE`

The V2 Focus surface now preserves the editorial clarity of the pair while adding direct spatial causality. The distorted source field, curved-reflection relation, and cylindrical member are visible as one system. MATCHING makes legibility belong to the cylindrical member; OTHER keeps both members valid while breaking registration.

The real Three.js reflection contract remains intact. Current SwiftShader evidence still contains runtime debt and must not be read as a production-device performance certification.

### Coupler · Virginia

Verdict: `V2_PILOT_PROMOTION_CANDIDATE`

V2 now provides a stronger embodied relation than the V1 explanatory schematic: the two named heads approach, the rotary-hook/latch relation registers, and pull is carried through the locked pair. The pair identity rail removes ambiguity without replacing the mechanism.

### Ombak · Bali

Verdict: `V2_PILOT_PROMOTION_CANDIDATE`

The previous presentation blocker is closed. Focus now keeps `PENGUMBANG` and `PENGISUP` evaluator-visible while the scene preserves the stronger V2 temporal mechanism: two synthetic tuned sources generate a shared visible/audible beat envelope.

The truth boundary remains unchanged:
- default study MATCHING = `7.0 Hz`
- default study OTHER = `12.0 Hz`
- no universal Bali-wide beat-rate claim
- no archive recording reproduced
- audio remains synthetic and user-initiated.

## Pattern verdict

`THE RELATIONAL PAIR REMAINS THE PRODUCT` is now demonstrated across three materially different runtime classes:
- optical / spatial
- mechanical / contact / load
- temporal / resonance

The reusable family-level presentation grammar is:

`PAIR MEMBER A → RELATION → PAIR MEMBER B`

with the memorable moment remaining owned by the relation rather than by generic 3D decoration.

## Runtime boundary

Runtime remains bounded-pass for the three-pilot evidence set, but scaling debt is open.

Latest observed headless Chromium / SwiftShader findings:
- Anamorphosis: max long task `268 ms` in this run
- Coupler: `0 ms`
- Ombak: `81 ms`

These values are diagnostic observations, not generalized user-device thresholds.

The production build still emits a large initial JavaScript payload dominated by the shared React / R3F / Three runtime. The current architecture must not simply absorb 21 additional scene modules into the same eager graph.

## Gate closeout

`V2_1_BROWSER_RUNTIME = PASS_WITH_MEASURED_DEBT`

`V2_1_FOCUS_EXPERIENCE = PASS`

`ANAMORPHOSIS_V2 = PROMOTION_CANDIDATE`

`COUPLER_V2 = PROMOTION_CANDIDATE`

`OMBAK_V2 = PROMOTION_CANDIDATE`

`V2_3_PILOT_PATTERN_VALIDATED = TRUE`

`V2_FAMILY_PROMOTION_TO_V1_MAIN = NOT_AUTHORIZED`

`V2_REMAINING_21_FAMILIES = NOT_AUTHORIZED`

## Next exact gate

`V2_2_SCALING_ARCHITECTURE_GATE`

Before any 21-family expansion, prove on the same three pilots that:
1. active scene/runtime loading does not require eagerly bundling every family scene;
2. family modules can be loaded independently behind one shared product shell;
3. the one-active-Canvas law remains intact;
4. browser/runtime, reduced-motion, keyboard and Focus evidence remain green;
5. build output reports the new chunk topology and measured before/after sizes without inventing arbitrary performance guarantees.
