# RELATIONAL KEY V2.1 — Targeted Experience Rework 001

Input evidence: `V2_1_RUNTIME_AND_EXPERIENCE_COMPARE_001.md`

Authorization is bounded to the three V2 pilots. V1 remains immutable.

## Anamorphosis · Paris

Replace the expensive but visually weak cube-reflection implementation with a procedural catoptric rectification visualization that makes the pair consequence unmistakable:

`DISTORTED SOURCE FIELD → CYLINDRICAL MEMBER → LEGIBLE REGISTERED FORM`

The cylinder remains 3D and directly draggable. The visual is explicitly a procedural study, not a calibrated physical-optics solver and not an archival reproduction.

Success condition: MATCHING visibly resolves a form on the cylinder that OTHER does not.

## Coupler · Virginia

Increase archive/mechanism specificity without adding a physics engine:

- shank / head
- opposing jaw surfaces
- rotary hook
- pivot pin
- latch/catch cue
- pair-spanning load path after lock

Success condition: approach → contact → hook rotation → lock → pull visibly reads as one causal mechanism; geometry must not read as two generic boxes.

## Ombak · Bali

Preserve the repaired semantic contract:

- MATCHING default = 7 Hz
- OTHER default = 12 Hz

Upgrade the visual relation from one generic line to two tuned source fields plus an emergent envelope / center phenomenon. Audio remains synthetic and user-initiated.

Success condition: the memorable third temporal phenomenon visibly belongs between the two members and disappears / degrades when the relationship is OTHER.

## Runtime constraints

- exactly one active Canvas
- no new dependencies
- `frameloop="demand"` preserved
- no continuous invalidation when a scene is settled
- reduced motion preserved
- mobile 390 px must not regress
- no Vercel preview

## Exit

Rerun both:

1. V2 React/R3F build + relational contract CI
2. exact-browser V1 ↔ V2 runtime comparison

Then perform a new screenshot-level V1 ↔ V2 experience comparison.

`RUNTIME_PASS != EXPERIENCE_ADVANTAGE_PASS`.
