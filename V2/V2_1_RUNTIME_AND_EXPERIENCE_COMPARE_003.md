# RELATIONAL KEY V2.1 — Human V1 ↔ V2 Comparison 003

Date: 2026-09-04
Branch: `v2/archive-interaction-3d-matrix-001`

## Exact evidence head

`1c0ac34b6c9ed03e064e018e0328a5d266513a10`

## CI / artifact identity

Build / relational contract:
- run `33903230822`
- PASS

Exact-browser + Focus Experience:
- run `33903230787`
- PASS
- artifact `9948489266`
- artifact digest `sha256:06240774013867f91ec01663eb289d755c195f1ea8c9f3f5fc1cc0f19e287e45`

Focus capture verdict:
`FOCUS_EXPERIENCE_CAPTURE_PASS_PENDING_HUMAN_V1_V2_COMPARISON`

All three Focus surfaces pass:
- deterministic pilot addressing
- OTHER / MATCHING
- one Canvas
- 1440 px no horizontal overflow
- 390 px no horizontal overflow
- no Focus console/page errors

## Runtime movement

Anamorphosis bounded reflection work improved materially under the same headless Chromium / SwiftShader class of evidence:
- prior targeted-rework run: max observed long task `365 ms`
- first Focus-gate attempt: `263 ms`
- current exact run: `233 ms`

This is an observed improvement, not a generalized performance certification or SLA. A long task is still present and remains runtime debt.

Current runtime findings:
- Anamorphosis: max observed long task `233 ms`, ~6 MiB JS heap
- Coupler: `0 ms` max observed long task, ~8 MiB JS heap
- Ombak: `0 ms` max observed long task in this run, ~7 MiB JS heap

Bundle debt remains open:
- one production JS chunk ~`1.097 MB` minified / ~`302 KB` gzip
- code splitting remains required before 24-family industrialization.

## Human comparison

### Anamorphosis · Paris

Verdict: `V2_PILOT_PROMOTION_CANDIDATE`

Why:
- Focus removes the laboratory chrome that previously distorted the comparison.
- V2 places the distorted field and cylindrical member in one spatial scene.
- MATCHING makes the registered result legible on the cylindrical member itself.
- OTHER preserves both members while visibly breaking registration.
- the real Three.js reflection path remains present behind the bounded procedural legibility layer.

V1 still has a strong editorial before/after composition, but V2 now adds direct spatial causality without losing the pair-owned reveal.

Remaining non-blocking debt:
- the 233 ms SwiftShader long task should continue to be tracked;
- bundle architecture is unresolved globally.

### Coupler · Virginia

Verdict: `V2_PILOT_PROMOTION_CANDIDATE`

Why:
- V2 now exposes jaw / hook / pivot / latch structure rather than generic boxes.
- OTHER shows contact without a shared lock.
- MATCHING + pull makes the relation carry force through the coupled pair.
- the family-level Focus surface makes the mechanical causality more immediate than the previous lab view.

V1 remains an elegant explanatory schematic, but V2 now provides the stronger embodied relation.

### Ombak · Bali

Verdict: `V2_TARGETED_PRESENTATION_REWORK_REQUIRED`

Why:
- V2 has the stronger emergent temporal mechanism: two synthetic sources, visible paired fields, user-initiated audio, and a central shared envelope.
- semantic preservation is correct: default `7 Hz MATCHING`, `12 Hz OTHER`.
- however, V1 visibly names the culturally specific pair `Pengumbang / Pengisup`, while the current V2 Focus view presents the two tuned members more generically.
- this makes the mechanism stronger but the archive/cultural identity weaker at first glance.

Required smallest repair:
- make both pair-member identities evaluator-visible in Focus;
- retain `PENGUMBANG` and `PENGISUP` for Ombak;
- do not turn the labels into a claim that 7 Hz is universal;
- keep the synthetic-audio truth boundary unchanged.

## Reusable pattern discovered

For family-level V2 presentation, the product grammar should be visible, not merely inferred:

`PAIR MEMBER A → RELATION → PAIR MEMBER B`

Authorized bounded presentation pattern:
- Focus renders explicit member A, relation, and member B identity;
- identities are family data, not hard-coded scene decoration;
- Ombak uses `PENGUMBANG → SHARED BEAT ENVELOPE → PENGISUP`;
- Anamorphosis uses `DISTORTED FIELD → CURVED REFLECTION → CYLINDRICAL REFLECTOR`;
- Coupler uses `PIVOTING HEAD → ROTARY HOOK / LATCH → MATING HEAD`;
- exact-browser Focus evidence must assert the rendered identity rail in desktop and mobile contexts.

## Gate state

`V2_1_BROWSER_RUNTIME = PASS`

`V2_1_FOCUS_EXPERIENCE_CAPTURE = PASS`

`ANAMORPHOSIS_V2 = PROMOTION_CANDIDATE`

`COUPLER_V2 = PROMOTION_CANDIDATE`

`OMBAK_V2 = TARGETED_PRESENTATION_REWORK_REQUIRED`

`V2_3_PILOT_PATTERN_VALIDATED = NOT_YET`

`V2_REMAINING_21_FAMILIES = NOT_AUTHORIZED`

Next exact gate:

`PAIR_MEMBER_IDENTITY_RAIL → FOCUS_CAPTURE_002 → HUMAN_V1_V2_COMPARISON_004`
