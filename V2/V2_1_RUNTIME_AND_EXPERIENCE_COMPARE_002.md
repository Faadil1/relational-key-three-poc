# RELATIONAL KEY V2.1 — Runtime + Experience Compare 002 / Focus Rework Contract

Date: 2026-09-04
Branch: `v2/archive-interaction-3d-matrix-001`

## Tested head before this rework

`9fdc2769be960b02c425c461467dbdc3c1efa793`

## Evidence identities

Build / relational contract:
- run `33901562815`
- PASS

Exact-browser V1 ↔ V2:
- run `33901562388`
- PASS
- artifact `9947868092`
- digest `sha256:98a346fe0ddfb8a0b73d5fbd492f9d3650eaa7edde72589bcdd5b6c68a656b3b`

No Vercel Preview was created.

## Runtime result

All three pilots remain browser-runtime ready within the tested scope:
- deterministic OTHER / MATCHING transitions
- native keyboard activation
- one WebGL canvas
- 390 px mobile no-overflow
- reduced-motion branch
- no V2 console/page/runtime errors

Observed runtime notes under headless Chromium SwiftShader:
- Anamorphosis: max long task `365 ms`, ~7 MiB JS heap → WARN / runtime debt
- Coupler: max long task `0 ms`, ~7 MiB JS heap
- Ombak: max long task `61 ms`, ~8 MiB JS heap
- production bundle remains ~`1.096 MB` minified / ~`301 KB` gzip in one large chunk

## Human V1 ↔ V2 comparison after targeted scene rework 001

### Anamorphosis

Mechanism improvement: PASS.

The V2 pair now makes the registered / unregistered state directly legible on the cylindrical member and preserves a real Three.js `CubeCamera` / `WebGLCubeRenderTarget` reflection path. However, the current screenshot is still embedded in the pilot laboratory shell and the SwiftShader startup long task regressed relative to the first browser pass.

### Coupler

Mechanism improvement: PASS.

The V2 coupler now exposes jaw / hook / pivot / latch geometry and a shared load path. This is materially more causal and mechanically specific than the first V2 build. The remaining comparison gap is presentation: V1 is shown as a finished family experience while V2 is still shown inside an assurance laboratory.

### Ombak

Mechanism improvement: PASS.

V2 preserves the canonical study relation (`7 Hz MATCHING`, `12 Hz OTHER`), user-initiated synthetic audio and a visibly emergent third field between the tuned members. As with Coupler, the remaining direct comparison gap is primarily presentation context rather than relation logic.

## Collision discovered

`LAB / ASSURANCE SURFACE != FAMILY EXPERIENCE SURFACE`

The current V2 shell is deliberately optimized for evidence, controls and truth boundaries. Comparing its full-page screenshot directly against V1 family pages confounds mechanism quality with evaluation-shell chrome.

## Authorized bounded rework 002

Add a deterministic **Focus Experience** presentation mode without changing the relational mechanism:

- addressable as `?focus=1&pilot=<pilot-id>`;
- family-specific title, memorable moment and pair law are evaluator-visible;
- lab tabs are removed from focus presentation;
- scene becomes the dominant visual surface;
- relation controls and truth boundary remain available;
- one Canvas only;
- no package additions;
- no V1/main mutation;
- exact-browser harness must capture focus OTHER / MATCHING and 390 px mobile for all three pilots.

Anamorphosis may additionally defer the low-resolution CubeCamera capture until an actual relation change and reduce bounded geometry / cube-target cost. The real reflection path must remain present; the contract must not be weakened.

## Gate state

`V2_1_BROWSER_RUNTIME = PASS`

`V2_1_MECHANISM_ADVANTAGE = PASS_FOR_3_PILOTS`

`V2_1_PRESENTATION_ADVANTAGE = PENDING_FOCUS_CAPTURE`

`V2_FAMILY_PROMOTION = NOT_AUTHORIZED`

`V2_REMAINING_21_FAMILIES = NOT_AUTHORIZED`

Next exact gate:

`FOCUS_EXPERIENCE_CAPTURE_001 → HUMAN_V1_V2_COMPARISON_003`
