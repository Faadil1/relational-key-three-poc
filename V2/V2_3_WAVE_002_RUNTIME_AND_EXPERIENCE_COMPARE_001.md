# RELATIONAL KEY V2.3 — Wave 002 Runtime + Experience Closeout

Date: 2026-09-05

## Verdict

**V2_3_WAVE_002 = PASS**

Wave 002 remains bounded to exactly three added families:

1. `astrolabe-isfahan`
2. `funicular-valparaiso`
3. `music-box-sainte-croix`

All three are now **V2 promotion candidates** after exact build, browser, Focus, scaling, mobile, reduced-motion and human V1↔V2 comparison gates.

The frozen V1 production collection is unchanged. `PROMOTION_CANDIDATE` does not mean merged to V1, deployed, or production-promoted.

## Product invariant

**THE RELATIONAL PAIR REMAINS THE PRODUCT.**

For every Wave 002 family:

`PAIR MEMBER A → RELATION → PAIR MEMBER B`

Both base members remain explicit and persistent. The relation deepens the response without becoming a third hero object.

Canonical Wave 002 pair contracts:

- Astrolabe — `RETE / STAR MAP → SHARED AXIS + RELATIVE ROTATION → LATITUDE PLATE / LOCAL HORIZON`
- Funicular — `CAR A POSITION ↔ SHARED FUNICULAR / INVERSE POSITION ↔ CAR B POSITION`
- Music Box — `PINNED CYLINDER / STORED GEOMETRY → PIN-TO-TOOTH CONTACT → TUNED COMB RESPONSE`

The Funicular contract remains deliberately non-binary. Its valid relation is continuous and reversible; V2 does not falsify it into MATCHING/OTHER success/failure semantics.

## Exact implementation and evidence identity

Final exact product/evidence head:

`a23c75112bcbb5cfd865c17c40971e73f6bfec1e`

Parent head before the bounded Music Box witness refinement:

`bbcb423893c66272029f7be2908f059b8148c74b`

Final build / relational contract run:

- GitHub Actions `V2 React R3F Pilot CI` run `33951845354` — **SUCCESS**
- npm audit: **0 vulnerabilities**
- relational contract: `V2_CONTRACT_PASS`
- scaling build: `SCALING_ARCHITECTURE_BUILD_PASS`

Final exact browser / Focus / scaling run:

- GitHub Actions `V2 Exact Browser Runtime Compare` run `33951845352` — **SUCCESS**
- runtime verdict: `BROWSER_RUNTIME_PASS_PENDING_HUMAN_V1_V2_EXPERIENCE_COMPARISON`
- Focus capture: **PASS pending human comparison**, then human-closeout PASS
- scaling verdict: `SCALING_ARCHITECTURE_BROWSER_PASS`
- evidence artifact: `9965133702`
- artifact SHA-256: `433add9ba598c8d5421faf98809a006c1849227a1130cc550a19bec96f35813f`

The human V1↔V2 comparison was completed against screenshots from that exact artifact.

## Technical assurance

Final nine-family architecture evidence:

- nine lazy family entries
- one shared `FamilyCanvas`
- one active WebGL canvas / runtime during direct Focus and sequential switching
- no unrelated family entry loaded before its selection
- `canvasPeak = 1`
- all nine tested families fit the `390 px` mobile viewport without horizontal overflow
- reduced-motion path PASS for all nine tested families
- keyboard/focus evidence retained in the exact browser harness
- V2 console/runtime/page errors: none in the tested Wave 002 paths
- Wave 001 + Wave 002 two-base-card markers: CI enforced
- Wave 002 archive-specificity markers: CI enforced
- nine distinct relational laws: CI enforced
- Funicular non-binary control contract: CI enforced

Measured build at the final exact head:

- initial static JS: `218,757 bytes`
- shared deferred `FamilyCanvas` runtime: `880,851 bytes`
- Astrolabe entry: `4,498 bytes`
- Funicular entry: `3,525 bytes`
- Music Box entry: `4,759 bytes`
- complete JS build: `1,138,674 bytes`

Runtime observations remain diagnostic rather than invented SLA thresholds. The exact browser gate produced no blocking Wave 002 runtime finding.

## Wave 002 experience comparison

### Astrolabe · Isfahan

V2 preserves the two instrument members as separate readable objects and makes the relationship continuous: the rete/star map rotates relative to a stationary latitude/local-horizon plate around the shared axis. `LOCAL PLATE` and `OTHER PLATE` remain valid instrument contexts rather than right/wrong states.

Human V1↔V2 comparison found the V2 relation more spatially legible while preserving the truth boundary: this is a structural translation, not a complete astronomical simulator.

**Verdict: `V2_PILOT_PROMOTION_CANDIDATE`**

### Funicular · Valparaíso

V2 preserves simultaneous two-member visibility and the exact inverse positional law:

`B = 1 - A`

Agency remains symmetric: direct movement from either side determines the other. The memorable moment belongs to the pair because neither car alone can express the equal/opposite relation.

The comparison explicitly rejects a false success/failure model. `DEFAULT A LOW / B HIGH` and `SWAPPED A HIGH / B LOW` are two valid states of the same continuous relation.

**Verdict: `V2_PILOT_PROMOTION_CANDIDATE`**

### Music Box · Sainte-Croix

The first exact technical/browser comparison was green, but the first human visual comparison placed Music Box on **HOLD** because the `pin → tooth → response` event was not visually strong enough; the status text carried too much of the causal proof.

Bounded repair at final head `a23c75112bcbb5cfd865c17c40971e73f6bfec1e`:

- retained the same editorial cylinder patterns and contact-window logic;
- retained the same angles, engage semantics and truth boundary;
- made the actually active comb tooth visibly brighter/emissive;
- added a restrained `CONTACT_EVENT_WITNESS` only when a real pin is inside the existing contact window;
- the witness crosses the pair at the active tooth channel so the causal event is visible rather than merely described;
- no historical tune, authentic Paillard tuning/timbre, sampled recording or unrestricted physics claim was introduced.

The full exact build/browser matrix was rerun after that repair and remained green. Human inspection of the final `Cylinder A at 52°` evidence shows the active tooth and cross-pair contact event visibly registering.

**Verdict: `V2_PILOT_PROMOTION_CANDIDATE`**

## Final Wave 002 state

- `ASTROLABE_V2 = PROMOTION_CANDIDATE`
- `FUNICULAR_V2 = PROMOTION_CANDIDATE`
- `MUSIC_BOX_V2 = PROMOTION_CANDIDATE`
- `V2_3_WAVE_002_BROWSER_RUNTIME = PASS_WITH_MEASURED_DIAGNOSTICS`
- `V2_3_WAVE_002_FOCUS_EXPERIENCE = PASS`
- `V2_3_WAVE_002_SCALING = PASS`
- `V2_3_WAVE_002_TWO_BASE_CARDS = CI_ENFORCED`
- `V2_3_WAVE_002_ARCHIVE_SPECIFICITY = CI_ENFORCED`
- `V2_3_WAVE_002 = PASS`

Together with V2.1 and Wave 001, the bounded V2 branch now has **9 promotion candidates**:

1. Anamorphosis · Paris
2. Coupler · Virginia
3. Ombak · Bali
4. Kento · Japan
5. Stereoscopy · UK
6. Signal · Nigeria
7. Astrolabe · Isfahan
8. Funicular · Valparaíso
9. Music Box · Sainte-Croix

## Non-authorization boundary

This closeout does **not** authorize:

- V2 replacement of V1 production;
- mutation of frozen V1 R5 truth;
- a 24-family mass React/R3F port;
- 24 simultaneous live WebGL canvases;
- automatic build expansion across the remaining 15 families;
- archive asset reuse without explicit rights/product need;
- a routine Vercel preview or production deployment.

## Next exact gate

The next gate is **selection only**:

`V2_3_WAVE_003_SELECTION_COLLISION_AND_ARCHIVE_REFRESH`

It must choose a small bounded Wave 003 from the remaining 15 families, collision-check each mechanism against all nine existing V2 promotion candidates, refresh archive/rights evidence for only the selected families, and explicitly authorize builds before any new family implementation begins.
