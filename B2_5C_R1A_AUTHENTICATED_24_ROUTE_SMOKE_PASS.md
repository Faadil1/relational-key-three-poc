# RELATIONAL KEY — B2.5C-R1A

Date: 2026-08-29
Gate: `B2.5 — 24-FAMILY COLLECTION REGRESSION`
Output: `AUTHENTICATED IN-BROWSER 24-ROUTE SMOKE CHECK`

# VERDICT

# **PASS / VALIDATED TECHNICAL ✅**

B2.5C is now technically closed.

# CANONICAL INVARIANTS

**THE RELATIONAL PAIR REMAINS THE PRODUCT.**

`PAIR MEMBER → RELATION → OTHER MEMBER RESPONSE`

Expansion laws remain binding:
- **EXPANSION MUST DEEPEN THE RELATION, NOT REPLACE IT.**
- **TECHNOLOGY SERVES THE RELATIONAL MECHANISM.**
- **THE MEMORABLE MOMENT MUST BELONG TO THE PAIR.**

# DEPLOYMENT PROOF

Source-neutral retrigger commit:
`5d3ed9de5fbbf0427f1ad0eac5b6910b4a220f5a`

READY deployment:
`dpl_ERZaEXq8vs4knBEsgdUTNshmoCaB`

Preview hostname:
`relational-key-collectionrelational-key-collection-3w617k1qt.vercel.app`

Vercel build metadata/logs prove:
- repository `Faadil1/relational-key-three-poc`;
- branch `collection/global-refinement-1`;
- exact commit `5d3ed9de...`;
- `vercel build` completed successfully;
- deployment completed;
- deployment state `READY`;
- GitHub Vercel status `success`.

# AUTHENTICATED USER SMOKE EVIDENCE

The user executed the same-origin smoke check inside the authenticated READY preview.

Observed route matrix:
- 24 / 24 canonical family routes returned HTTP `200`;
- 24 / 24 route responses contained `BACK TO ATLAS`;
- 9 / 9 Balance-Gate wrappers referenced `candidate.html`;
- 9 / 9 Balance-Gate `candidate.html` targets returned HTTP `200`.

Live registry checks:
- `/collection/families.json` HTTP `200`;
- schema version `1.3`;
- validated count `24`;
- candidate count `0`;
- exactly 24 families;
- exactly 24 unique IDs;
- exactly 24 unique routes;
- distribution = Africa 4 / Asia 4 / North America 4 / South America 4 / Oceania 4 / Europe 4.

Root product-law check:
- `The pair is the product.` = PASS.

# ROOT24 SCRIPT FALSE — DIAGNOSTIC ONLY

The initial browser script reported `root24 = false` while all live route/registry checks passed.

This was a test artefact, not a root defect:
- the script used `document.body.innerText`;
- the Atlas source places `24 PAIR-NATIVE FAMILIES` in `.topCenter`;
- the responsive source rule under `max-width:1100px` sets `.topCenter{display:none}`;
- opening DevTools can reduce the page viewport below that breakpoint;
- `innerText` excludes content hidden by `display:none`.

The deployed commit is proven exact and the source root contains the 24-family label. Therefore the hidden responsive header must not override the direct live proof of registry 24/24 plus route 24/24.

# B2.5C TECHNICAL CLOSURE

Evidence separation:
- SOURCE 24-family integration: PASS;
- SOURCE scope / 9 candidate immutability: PASS;
- Vercel build-rate recovery: PASS;
- exact integrated commit deployed: PASS;
- READY deployment: PASS;
- authenticated live registry: PASS;
- live canonical routes: PASS 24/24;
- Back-to-Atlas route contract: PASS 24/24;
- new candidate targets: PASS 9/9;
- USER visual coherence / responsive visual quality: NOT YET RUN;
- USER causal family regression: NOT YET RUN;
- VALIDATED family count: 24 unchanged.

# NEXT EXACT OUTPUT

# **B2.5D — 24-FAMILY ATLAS USER VISUAL + NAVIGATION GATE**

B2.5D must prove visual/navigation coherence on:
1. desktop Atlas;
2. true ~390 px viewport;
3. 24-family discoverability and continental filtering;
4. wrapper framing and Back-to-Atlas behavior;
5. no horizontal overflow / clipped critical UI;
6. original pair-product idea remains perceptually dominant;
7. advanced visual expansion remains subordinate to mechanism identity.

After B2.5D PASS, execute the B2.5A causal USER regression batches A–D.

PR #21 remains `DRAFT / DO NOT MERGE`.
G4 remains PARKED until B2.5 closes.
