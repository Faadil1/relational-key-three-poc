# ROUND 13M.4 — BOULLE IMPLEMENTATION GATE

Date: 2026-08-25
Branch: `round13m/complement-memory-boulle`

## Edition

**COMPLEMENT MEMORY — FRANCE / BOULLE — RECIPROCAL CUT REGISTER**

Law:
**ONE SHARED CUT PRODUCES TWO COMPLEMENTARY INVERSES.**

Proof:
`SHEET A ≠ SHEET B → STACK → SHARED CUT → SEPARATE → EXCHANGE → PARTIE ↔ CONTREPARTIE`

Pair:
- Card A — `PARTIE / DARK GROUND + LIGHT INLAY`
- Card B — `CONTREPARTIE / LIGHT GROUND + DARK INLAY`
- Gap — `SHARED CUT / RECIPROCAL EXCHANGE FIELD`

Signature:
`ALIGN → STACK → CUT → SEPARATE → EXCHANGE → INVERT → REGISTER → CONTINUE`

## Implementation commit

`d9067507c1db464a97defdbdd63346b9d82a3a29`

## Visual construction

- two cards visible immediately;
- Card A begins as a dark editorial material field;
- Card B begins as a warm metal-like editorial field;
- motif is an original vector shape created for this prototype, not copied Boulle ornament;
- no historical photograph is used as the main interface skin;
- no real tortoiseshell / wildlife material is used or represented as a recommended modern material.

## SHARED CUT

Sequence:
`ALIGN → STACK → CUT → SEPARATE → EXCHANGE → INVERT → REGISTER → CONTINUE`

Behavior:
1. two different cards move toward relation;
2. two center material layers stack;
3. one identical editorial cut path resolves across both cards + center layers;
4. layers separate;
5. reciprocal exchange cue appears;
6. the same vector region fills with opposite material roles on the two cards;
7. registered state explicitly reads `PARTIE ↔ CONTREPARTIE`;
8. Card A and B remain visibly distinct rather than converging to sameness.

## OTHER CUT

- both material fields remain valid;
- center layers use offset / non-shared path resolution;
- main card cut paths remain partial / displaced;
- no inlay exchange appears;
- `PARTIE ↔ CONTREPARTIE` does not register;
- no red X / invalid material state.

## Distinction from existing families

- Fès = pieces fit a selected geometry.
- Kento = print/transfer registration.
- Stereoscopy = visual disparity/fusion.
- Boulle = one shared division produces reciprocal positive/negative material outcomes.

## Historical grounding / ethical boundary

Source mechanism:
- The Metropolitan Museum of Art documents Boulle work as stacked sheets cut according to one design, separated and recombined as reciprocal material versions.

Primary references:
- https://www.metmuseum.org/art/collection/search/202281
- https://www.metmuseum.org/art/collection/search/207667

Historical tortoiseshell is evidence only.

Prototype rules:
- no wildlife material is used;
- no wildlife material procurement/use is recommended;
- rendered dark material is an abstract editorial simulation;
- timing, path geometry, layering and exchange are interface translations;
- no exact cutting tools, dimensions or modern fabrication recipe are claimed.

## Local validation

PASS before source push:
- duplicate DOM IDs: none;
- `SHARED CUT` mode present;
- `OTHER CUT` mode present;
- 8-stage signature present;
- registered reciprocity is target-only;
- no red invalidation;
- responsive pair layout present;
- inline JavaScript extracted and `node --check` PASS.

## Status

# **IMPLEMENTED / LOCAL STRUCTURE + SYNTAX PASS**

`IMPLEMENTED ≠ READY ≠ LIVE CANDIDATE ≠ VISUAL PASS`.

## Next required output

# ROUND 13M.5 — DEDICATED VERCEL LIVE CANDIDATE

Use a dedicated audit project. Do not overwrite any existing family live.

Suggested project name:
`relational-key-complement-memory-boulle-v13m1`

Required sequence:
1. create/connect project to `Faadil1/relational-key-three-poc`;
2. trigger preview from `round13m/complement-memory-boulle`;
3. verify deployment `READY`;
4. directly verify served HTML is Boulle Reciprocal Cut Register;
5. record source/live sync;
6. user records SHARED CUT + OTHER CUT;
7. visual decision `PASS / PATCH / REJECT`;
8. keep PR Draft until visual pass.
